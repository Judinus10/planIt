spring.application.name=planIt

# Database Configuration
spring.datasource.url=jdbc:mysql://mysql:3306/planit?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=ud
spring.datasource.password=pw1234

# JPA (Hibernate) Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Optional: Prevent connection pool timeout issues
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Server port 
server.port=8080
